// class WebSockets {
//   //array of active tokens
//   tokens = [];

//   connection(client) {
//     client.on("active", (token) => {
//       client.join(token);
//     });

//     client.on("deactive", (token) => {
//       client.leave(token);
//     });
//   }
// }

// module.exports = new WebSocket();
